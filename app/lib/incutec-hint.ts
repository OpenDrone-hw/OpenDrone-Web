/** localStorage key - once the visitor has clicked through to the Incutec page
 *  (via the header mark or the hero "Who's incutec?" hint) the hint is retired
 *  for good. Shared by the Header (writes it) and the homepage (reads it). */
export const INCUTEC_HINT_SEEN_KEY = 'od-incutec-seen';
